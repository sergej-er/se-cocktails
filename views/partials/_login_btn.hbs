<a href={{authLink}}
  class="login-btn max-w-sm flex-grow flex justify-center {{{colorClass}}} text-white items-center p-6 rounded-lg shadow-xl mx-10 sm:mx-0 mb-4">
  <div>
    <i class="{{{fa-icon}}} text-lg"></i>
  </div>
  <div class="ml-6">
    <p class="text-lg">{{buttonText}}</p>
  </div>
</a>